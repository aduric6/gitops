apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Optional: set the target namespace (app still has zero resources)
namespace: gitops-namepsace-test

# No resources on purpose — this is a heartbeat only
resources: []
